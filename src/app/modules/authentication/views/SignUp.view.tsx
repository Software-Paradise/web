import React from 'react';
import { ScrollLayout } from 'library/layouts';

const SignUpView: React.FunctionComponent = () => {
	return <ScrollLayout className="signin-view">SignUpView CONTENT</ScrollLayout>;
};

export default React.memo(SignUpView);
